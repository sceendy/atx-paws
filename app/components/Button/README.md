Buttons:

```jsx

<Button 
  type="primary"
  onClick={() => setState({ clicked: true })}
  text="Submit a form"
/>

<Button 
  type="primary-inverted"
  onClick={() => setState({ clicked: true })}
  text="Open Modal"
/>
```